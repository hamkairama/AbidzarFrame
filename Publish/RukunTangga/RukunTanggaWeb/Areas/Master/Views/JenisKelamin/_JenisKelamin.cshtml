@model AbidzarFrame.Master.Interface.Dto.JenisKelaminResult
@{
    Layout = null;
}

@Html.AntiForgeryToken()
@Html.Hidden("hiddenActionGrid", (object)ViewBag.ActionGrid)

<form id="FormObject">
    <fieldset>
        <div class="col-md-12">
            @{
                if ((object)ViewBag.ActionGrid == "Edit")
                {
                    <div class="row hidden">
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Id, new { @class = "form-control", @style = "width:100%", @maxlength = "50" })
                        </div>
                    </div>
                }
            }
            <div class="row">
                <div class="form-group">
                    @Html.LabelFor(m => m.JenisKelamin)
                    @Html.TextBoxFor(m => m.JenisKelamin, new { @class = "form-control", @style = "width:100%", @maxlength = "1" })
                    @Html.ValidationMessageFor(m => m.JenisKelamin, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="row">
                <div class="form-group">
                    @Html.LabelFor(m => m.Deskripsi)
                    @Html.TextBoxFor(m => m.Deskripsi, new { @class = "form-control", @style = "width:100%", @maxlength = "20" })
                    @Html.ValidationMessageFor(m => m.Deskripsi, "", new { @class = "text-danger" })
                </div>
            </div>
        </div>

    </fieldset>
</form>

